services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - transcript-data:/data
    restart: unless-stopped

volumes:
  transcript-data:
